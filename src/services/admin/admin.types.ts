export interface IAdmin {
    adminId: string;
    fullName: string;
    email: string;
    address: string;
    phone: string;
    avatarUrl?: string;
    publicId?: string;
    createdAt: string;
    updatedAt: string;
}